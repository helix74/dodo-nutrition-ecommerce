feat: homepage redesign — 9-section architecture + dataset import guide

Homepage completely redesigned with a new premium 9-section layout:

1. Hero Section — CMS-driven slider from Sanity Studio (heroSlideType schema)
   with trust strip (livraison, authentique, COD, retour)
2. Goal Navigator — tiles mapping fitness goals to categories
3. Featured Products — tabs (Best-sellers, New, Promos) with badges
4. CMS Banners — full-bleed edge-to-edge banner carousel
5. Packs and Deals — with savings badges
6. Categories — image grid with product counts from Sanity
7. Social Proof — rating summary + review cards
8. Brands Marquee — infinite auto-scroll CSS animation
9. Final CTA section

New components:
- GoalNavigator.tsx, BrandsMarquee.tsx, FeaturedPackCard.tsx, FinalCTA.tsx

Rewritten components:
- HeroSection (CMS slides + trust strip), FeaturedProducts (tabs + carousel),
  CategoryShowcase (images + productCount), BannerSection (full-bleed),
  FeaturedPacks (savings emphasis)

Deleted legacy components:
- TrustBar, WhyUsSection, PromoBanner, NewsletterSection, BrandsCarousel

Backend changes:
- New heroSlideType schema + registered in index.ts
- Sanity Studio restructured (Marketing / Catalogue / Commerce)
- New queries: ACTIVE_HERO_SLIDES_QUERY, NEW_PRODUCTS_QUERY, PROMO_PRODUCTS_QUERY
- Extended: ALL_CATEGORIES_QUERY (productCount), FEATURED_PACKS_QUERY (products[])
- Shop page supports ?goal= param for multi-category filtering
- AddToCartButton accepts custom label prop
- Brand schema: logo field title/description added
- Marquee CSS keyframes in globals.css

Dataset import tooling:
- data/template-products.csv — empty CSV with correct headers
- data/example-products.csv — 5 example products correctly formatted
- data/GUIDE-IMPORT-PRODUITS.md — strict guide for AI agent to convert
  old CSV to new format (category mapping, description rewrite rules,
  benefits cleanup, fake priceSlashed detection, SEO rules)

Language: Darija Arabic script mixed with French.
Mobile-first responsive design throughout.
TypeCheck: PASS | Build: PASS (26 pages)
